{
  "name": "knowledgeops-agent-typescript",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r build",
    "container:security-smoke": "node scripts/container-security-smoke.mjs",
    "contract:diff:live": "node scripts/contract-diff.mjs",
    "db:generate": "pnpm --filter @knowledgeops/api exec prisma generate --schema ../../prisma/schema.prisma",
    "db:integration": "node scripts/database-integration.mjs",
    "db:migrate": "node scripts/prisma-migrate.mjs",
    "db:schema:validate": "pnpm --filter @knowledgeops/api exec prisma validate --schema ../../prisma/schema.prisma",
    "db:seed-demo": "node scripts/bootstrap-demo-key.mjs",
    "e2e:smoke": "node scripts/e2e-smoke.mjs",
    "inventory:contracts": "node scripts/contract-inventory.mjs",
    "inventory:cutover-plan": "node scripts/cutover-plan-inventory.mjs",
    "inventory:deployment-security": "node scripts/deployment-security-inventory.mjs",
    "inventory:frontend-routes": "node scripts/frontend-route-inventory.mjs",
    "inventory:implementation-surface": "node scripts/implementation-surface-inventory.mjs",
    "inventory:prisma-tables": "node scripts/prisma-table-inventory.mjs",
    "inventory:all": "pnpm inventory:implementation-surface && pnpm inventory:contracts && pnpm inventory:prisma-tables && pnpm inventory:frontend-routes && pnpm inventory:cutover-plan && pnpm inventory:deployment-security",
    "load:gate": "node scripts/load-gate.mjs",
    "perf:smoke": "node scripts/perf-smoke.mjs",
    "security:audit": "pnpm --registry=https://registry.npmjs.org audit --audit-level high",
    "test": "pnpm --filter @knowledgeops/shared build && pnpm -r test",
    "test:coverage": "pnpm --filter @knowledgeops/shared build && pnpm --filter @knowledgeops/shared exec vitest run --coverage --coverage.include='src/**/*.ts' --coverage.exclude='src/**/*.spec.ts' --coverage.reporter=text --coverage.reporter=json-summary --coverage.reportsDirectory=coverage --coverage.thresholds.lines=90 --coverage.thresholds.statements=90 --coverage.thresholds.functions=90 --coverage.thresholds.branches=85 && pnpm --filter @knowledgeops/api exec vitest run --coverage --coverage.include='src/**/*.ts' --coverage.exclude='src/**/*.spec.ts' --coverage.reporter=text --coverage.reporter=json-summary --coverage.reportsDirectory=coverage --coverage.thresholds.lines=40 --coverage.thresholds.statements=39 --coverage.thresholds.functions=32 --coverage.thresholds.branches=24",
    "typecheck": "pnpm -r typecheck",
    "verify:local": "pnpm db:generate && pnpm db:schema:validate && pnpm typecheck && pnpm test:coverage && pnpm build && pnpm inventory:all && pnpm security:audit"
  },
  "dependencies": {
    "@prisma/client": "6.19.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.2",
    "@vitest/coverage-v8": "4.1.7",
    "typescript": "^6.0.3",
    "vitest": "^4.0.15"
  },
  "pnpm": {
    "overrides": {
      "fast-uri": "3.1.4",
      "vite": "8.0.16"
    },
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma"
    ]
  }
}
