{
  "name": "knowledgeops-agent-typescript",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r build",
    "contract:diff": "node scripts/contract-diff.mjs",
    "db:generate": "pnpm --filter @knowledgeops/api exec prisma generate --schema ../../prisma/schema.prisma",
    "db:validate": "node scripts/check-prisma-schema.mjs",
    "e2e:smoke": "node scripts/e2e-smoke.mjs",
    "frontend:contract": "node scripts/frontend-contract-smoke.mjs",
    "load:gate": "node scripts/load-gate.mjs",
    "maturity:gate": "node scripts/maturity-gate.mjs",
    "migration:readiness": "node scripts/migration-readiness.mjs",
    "parity": "node scripts/check-parity.mjs",
    "perf:smoke": "node scripts/perf-smoke.mjs",
    "sbom": "node scripts/sbom.mjs",
    "prod:gate": "pnpm parity && pnpm db:validate && pnpm contract:diff && pnpm frontend:contract && pnpm migration:readiness && pnpm maturity:gate && pnpm e2e:smoke && pnpm perf:smoke",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @knowledgeops/shared build && pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^24.10.2",
    "typescript": "^6.0.3",
    "vitest": "^4.0.15"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "esbuild"
    ]
  }
}
