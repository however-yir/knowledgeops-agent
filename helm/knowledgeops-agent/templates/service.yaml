apiVersion: v1
kind: Service
metadata:
  name: {{ include "knowledgeops-agent.fullname" . }}
  labels:
    {{- include "knowledgeops-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knowledgeops-agent.selectorLabels" . | nindent 4 }}
